enum VoteEnum {
  YES
  MAYBE
  NO
}

type User @model {
  id: ID!
  username: String!
  confirmed: Boolean
  vote: Vote @connection(name: "UserVote")
}

type Vote @model {
  id: ID!
  isComing: VoteEnum
  user: User @connection(name: "UserVote")
  createdAt: String
}
